server:
    port: 88
    servlet:
        context-path: /
spring:
    # MySQL
    datasource:
        url: jdbc:mysql://127.0.0.1:3306/example_security?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: root
    # Redis
    redis:
        database: 0
        host: 192.168.3.128
        password: m5dAyo5Hm5kG
        port: 6379
config:
    # 是否启用ansi控制台输出有颜色的字体，local环境建议开启，服务器环境设置为false
    enable-ansi: true
    # 请求日志在控制台是否格式化输出，local环境建议开启，服务器环境设置为false
    request-log-format: true
    # 响应日志在控制台是否格式化输出，local环境建议开启，服务器环境设置为false
    response-log-format: true
    # 登录设置
    login:
        token-valid-time: 60  # 登录token失效时间，单位分钟
